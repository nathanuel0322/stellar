import { StyleSheet } from "react-native";

export const groupstyles = StyleSheet.create({
    TextInput: {
      height: 100,
      flex: 1,
      padding: 10,
      textAlign: 'center',
      fontSize: 16,
    },
});